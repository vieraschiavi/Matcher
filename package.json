{
  "name": "matcher",
  "private": true,
  "version": "1.0.0",
  "description": "Matcher — app de citas con filtros que se respetan (edad, altura, postura política, equipo de fútbol del país) y planes pagos más baratos que la competencia. Web, Android e iOS.",
  "scripts": {
    "build:web": "cd webapp/frontend && npm install && npm run build",
    "sync:android": "npm run build:web && npx cap sync android",
    "sync:ios": "npm run build:web && npx cap sync ios",
    "init:android": "npx cap add android",
    "init:ios": "npx cap add ios",
    "apk:debug": "npm run sync:android && cd android && ./gradlew assembleDebug",
    "apk:release": "npm run sync:android && cd android && ./gradlew assembleRelease",
    "ios:abrir": "npm run sync:ios && npx cap open ios",
    "pc": "npm run build:web && electron .",
    "pc:windows": "npm run build:web && electron-builder --win --publish never",
    "pc:linux": "npm run build:web && electron-builder --linux --publish never",
    "pc:mac": "npm run build:web && electron-builder --mac --publish never"
  },
  "devDependencies": {
    "@capacitor/cli": "^6.2.0",
    "electron": "^32.2.7",
    "electron-builder": "^25.1.8"
  },
  "dependencies": {
    "@capacitor/android": "^6.2.0",
    "@capacitor/app": "^6.0.3",
    "@capacitor/app-launcher": "^6.0.4",
    "@capacitor/browser": "^6.0.6",
    "@capacitor/core": "^6.2.0",
    "@capacitor/ios": "^6.2.0"
  },
  "main": "electron/main.js",
  "author": "Matcher"
}
