<?xml version="1.0" encoding="utf-8"?>
<!--
  Nada de HTTP en claro. Sin esto, cualquier subdominio mal configurado o un
  redirect a http:// mandaría el token de sesión y las fotos por la red sin
  cifrar. Play además marca `usesCleartextTraffic` como hallazgo de seguridad.

  `localhost` sí se permite: es el propio WebView sirviendo el bundle, no sale
  del teléfono.
-->
<network-security-config>
    <base-config cleartextTrafficPermitted="false">
        <trust-anchors>
            <certificates src="system" />
        </trust-anchors>
    </base-config>
    <domain-config cleartextTrafficPermitted="true">
        <domain includeSubdomains="false">localhost</domain>
        <domain includeSubdomains="false">127.0.0.1</domain>
    </domain-config>
</network-security-config>
